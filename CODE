SKETCH.INO
#include <WiFi.h>

const char* ssid = "Wokwi-GUEST";
const char* password = "";

#define GAS_SENSOR 34
#define LED 2
#define BUZZER 4

void setup() {
  Serial.begin(115200);

  pinMode(LED, OUTPUT);
  pinMode(BUZZER, OUTPUT);

  WiFi.begin(ssid, password);

  Serial.print("Connecting to WiFi");

  while(WiFi.status() != WL_CONNECTED){
    delay(500);
    Serial.print(".");
  }

  Serial.println(" Connected!");
}

void loop() {

  int gasValue = analogRead(GAS_SENSOR);

  Serial.print("Gas Level: ");
  Serial.println(gasValue);

  if(gasValue > 600){

    digitalWrite(LED, HIGH);
    digitalWrite(BUZZER, HIGH);

    Serial.println("Gas Leakage Detected!");

  }
  else{

    digitalWrite(LED, LOW);
    digitalWrite(BUZZER, LOW);

    Serial.println("Safe");

  }

  delay(1000);
}


DIAGRAM.JSON

{
  "version": 1,
  "author": "Krithika.S",
  "editor": "wokwi",
  "parts": [
    { "type": "board-esp32-devkit-c-v4", "id": "esp", "top": -76.8, "left": 4.84, "attrs": {} },
    { "type": "wokwi-led", "id": "led1", "top": 6, "left": 234.2, "attrs": { "color": "red" } },
    {
      "type": "wokwi-buzzer",
      "id": "bz1",
      "top": -93.6,
      "left": 193.8,
      "attrs": { "volume": "0.1" }
    },
    { "type": "wokwi-gas-sensor", "id": "gas1", "top": 21.9, "left": -185, "attrs": {} }
  ],
  "connections": [
    [ "esp:TX", "$serialMonitor:RX", "", [] ],
    [ "esp:RX", "$serialMonitor:TX", "", [] ],
    [ "esp:5V", "gas1:VCC", "red", [ "h-115.05", "v-57.6" ] ],
    [ "gas1:GND", "esp:GND.1", "black", [ "h115.2", "v18.4" ] ],
    [ "gas1:AOUT", "esp:34", "green", [ "h144", "v-57.6" ] ],
    [ "led1:A", "esp:2", "green", [ "v76.8", "h-76.8" ] ],
    [ "led1:C", "esp:GND.3", "green", [ "v0", "h-66.8" ] ],
    [ "bz1:2", "esp:4", "green", [ "v28.8", "h-77.2" ] ],
    [ "bz1:1", "esp:GND.3", "green", [ "v-28.8", "h-67.2" ] ]
  ],
  "dependencies": {}
}






